services:
  pipeline-job:      # 给你的服务起个名字
    build: .         # 告诉它：就在当前目录找 Dockerfile 构建
    volumes:
      - .:/app       # 1. 把当前目录挂载进去（代码同步）
      - /app/.venv   # 2. 保护容器里的 Python 环境（匿名卷技巧）
    command: ["101"] # 3. 传给程序的参数（相当于 pipeline.py 101）